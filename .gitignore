node_modules/
.svelte-kit/
build/
dist/

# Downloaded raw data (regenerable from public sources; large)
data/raw/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/

# Claude Code local config (machine-specific permissions, etc.)
.claude/

# Supabase CLI artifacts
supabase/.temp/
supabase/.branches/

# Wikipedia-extracts dump for manual curation reference (CC BY-SA;
# regenerable via npm run wikipedia:summaries -- --full --out data/wikipedia-extracts.txt)
data/wikipedia-extracts.txt

# Regenerable raw exports — can be ~20MB+
data/exploration/great-morel-raw/
data/exploration/layer1-npn-raw.json
data/exploration/web-crawl-cache/
# Blog-evidence crawler local HTML cache (~37MB across ~170 pages;
# regenerable by re-running scripts/blog-evidence-crawl.cjs).
data/exploration/blog-cache/
# NPN raw export per species (multi-MB JSON; regenerable).
data/exploration/npn-validation-NE-raw-prunus-serotina.json

# Local DB backup snapshots (large; not source)
backups/
